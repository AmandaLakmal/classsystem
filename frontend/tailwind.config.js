/* =========================================================
   tailwind.config.js — DEPRECATED in Tailwind CSS v4
   
   In Tailwind v4, ALL configuration (content paths, dark mode,
   custom colors, theme extensions) is handled directly in CSS
   via @theme and @custom-variant directives inside index.css.
   
   This file is intentionally left as a comment-only stub
   to avoid confusion. Do NOT add v3-style config here.
   ========================================================= */