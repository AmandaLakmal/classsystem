/* =========================================================
   PostCSS Configuration — Tailwind CSS v4
   Use @tailwindcss/postcss (NOT the old tailwindcss plugin)
   ========================================================= */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}